# Configuration Guide

The project uses standard Linux utilities and Bash scripts.

Scripts do not require additional configuration files.

Before running scripts, make sure they have executable permissions:

    chmod +x scripts/*.sh

The project can be used on Linux and WSL2 environments.
